import React from "react";
import ExamSession from "../../components/ExamSession";

export default function ExamScreen({ navigation, route }) {
  return <ExamSession navigation={navigation} route={route} />;
}
